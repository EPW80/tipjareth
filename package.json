{
  "name": "tipflow",
  "version": "0.1.0",
  "private": true,
  "description": "TipFlow - decentralized creator tip platform",
  "workspaces": [
    "contracts",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n chain,api,web -c yellow,green,cyan \"npm run dev -w contracts\" \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "test": "npm run test:contracts && npm run test:backend && npm run test:frontend",
    "test:contracts": "npm run test -w contracts",
    "test:backend": "npm run test -w backend",
    "test:frontend": "npm run test -w frontend",
    "deploy:local": "npm run deploy:local -w contracts",
    "db:seed": "npm run db:seed -w backend",
    "build": "npm run build -w contracts && npm run build -w backend && npm run build -w frontend"
  },
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
